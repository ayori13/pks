# Практическое занятие № 8. Работа с базами данных. Подключение приложения к Firebase

# ЭФБО-09-23 Джанкёзов Руслан
# Цели:

-Подключить Flutter-приложение к Firebase через FlutterFire CLI.
-Освоить инициализацию firebase_core и работу с Cloud Firestore (cloud_firestore).
-Реализовать базовый CRUD (создание, чтение в реальном времени, обновление, удаление) для коллекции данных.
-Настроить минимальные правила безопасности Firestore для учебной среды.
-Сформировать практические навыки диагностики и устранения типовых ошибок подключения.

# Ход работы:

## 1. Скриншот настроенного проекта Firebase


## 2. Скриншот запущенного приложения с отображением списка.


## 3. Скриншот после добавления заметки.


## 4. Скриншот после редактирования.


## 5. Скриншот после удаления.



**Этапы подключения Firebase к проекту:**  
Настройка осуществлялась через Firebase CLI с выполнением команды `flutterfire configure`.  

**Последовательность действий:**  
После авторизации я выбрал проект Заметки в Firebase(также была возможность создать новый).  
Далее настроил поддерживаемые платформы, после чего конфигурационные файлы были сгенерированы автоматически.  

В коде я использовал пакеты `firebase_core` и `cloud_firestore`. Инициализацию Firebase выполнил в файле `main.dart` перед запуском приложения.  

**Примечание по безопасности:**  
На этапе разработки я применял упрощённые правила доступа к Firestore вида `allow read, write: if true`, что открывало полный доступ к данным.  
Для рабочего окружения необходимо заменить эти правила на строгие, с обязательным использованием аутентификации и проверкой прав пользователей.

Коллекция notes с полями:

title : string

content : string

createdAt : timestamp

updatedAt : timestamp
